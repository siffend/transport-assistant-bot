# Ядро ассистента работает на стандартной библиотеке Python 3.10+.
# Ниже — только то, что нужно дополнительным слоям.

flask>=3.0        # веб-демо и приём вебхука Telegram
openpyxl>=3.1     # пересборка базы знаний из исходного Excel (tools/build_kb.py)
gunicorn>=23.0    # запуск Flask-приложения на Render